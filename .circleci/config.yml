version: 2.1
jobs:
  build:
    docker:
      - image: alpine:3.15
    steps:
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
workflows:
  before-merging:
    jobs:
      - build
      - approval:
        type: approval
        filters:
          branches: /gh-readonly-queue\/main/
        requires:
          - build
      
